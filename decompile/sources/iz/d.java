package iz;

import androidx.recyclerview.widget.RecyclerView;

public interface d {
    int a(RecyclerView recyclerView, RecyclerView.v vVar);

    int b(RecyclerView recyclerView, RecyclerView.v vVar);
}
